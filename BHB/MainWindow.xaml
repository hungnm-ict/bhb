<Window x:Class="BHB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="BHB — Bit Heroes Bot"
        Height="700" Width="1000"
        Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="BHB — Bit Heroes Bot"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,12" />

        <!-- Window selector + action buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <ComboBox ItemsSource="{Binding AvailableWindows}"
                      SelectedItem="{Binding SelectedWindow}"
                      DisplayMemberPath="Title"
                      Width="280"
                      materialDesign:HintAssist.Hint="Select game window"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}" />
            <Button Command="{Binding RefreshWindowsCommand}"
                    Content="Refresh"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="8,0,0,0" />
            <Button Command="{Binding CaptureTestCommand}"
                    Content="Capture"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="8,0,0,0" />
            <Button Command="{Binding ClickTestCommand}"
                    Content="Click Center"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="8,0,0,0" />
            <Button Command="{Binding StartCommand}"
                    Content="Start"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="8,0,0,0" />
            <Button Command="{Binding StopCommand}"
                    Content="Stop"
                    Style="{StaticResource MaterialDesignRaisedDarkButton}"
                    Margin="8,0,0,0" />
            <TextBlock Text="{Binding CurrentState}"
                       VerticalAlignment="Center"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Margin="16,0,0,0" />
        </StackPanel>

        <!-- Capture preview -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                Margin="0,0,0,12">
            <Image Source="{Binding LastCapture}"
                   Stretch="Uniform" />
        </Border>

        <!-- Log output -->
        <TextBox Grid.Row="3"
                 Text="{Binding LogOutput}"
                 IsReadOnly="True"
                 Height="120"
                 VerticalScrollBarVisibility="Auto"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 FontFamily="Consolas"
                 FontSize="11"
                 materialDesign:HintAssist.Hint="Activity log" />
    </Grid>
</Window>
